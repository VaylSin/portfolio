{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ page.title }}</h1>

    {% for block in page.blocks %}
        {% include 'block/' ~ block.type ~ '.html.twig' with { 'block': block } %}
    {% endfor %}
{% endblock %}